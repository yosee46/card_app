
var dom = $(".card_count", "#card_<%= @user_card.card_id %>")
var val = dom.text()
val = Number(val) || 0
val = val+1
if (val === 1) {
    $(".delete_btn", "#card_<%= @user_card.card_id %>").val("減らす")
}
dom.text(val)
